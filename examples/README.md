# Examples

`sample_points.csv` demonstrates the minimum input expected by the points-only workflow.

Columns:

- `point_id`: unique point identifier;
- `latitude`: WGS84 latitude;
- `longitude`: WGS84 longitude;
- `heading`: optional camera direction in degrees.

The coordinates are demonstration inputs only and do not represent validated agricultural sites.
