# Generated outputs

This directory is reserved for generated metadata tables, candidate-point files, download logs,
and Street View images. Production outputs are excluded from version control.

Expected subdirectories:

```text
outputs/
├── field_centric_adaptive/
├── field_centric_one_per_field/
└── points_only/
```
